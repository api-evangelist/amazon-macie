{
  "$schema": "https://json-structure.org/meta/core/v0/#",
  "$id": "https://raw.githubusercontent.com/api-evangelist/amazon-macie/refs/heads/main/json-structure/amazon-macie-describe-buckets-request-structure.json",
  "name": "DescribeBucketsRequest",
  "description": "DescribeBucketsRequest schema from Amazon Macie API",
  "type": "object",
  "properties": {
    "criteria": {
      "allOf": [
        {
          "$ref": "#/components/schemas/BucketCriteria"
        },
        {
          "description": "The criteria to use to filter the query results."
        }
      ]
    },
    "maxResults": {
      "allOf": [
        {
          "$ref": "#/components/schemas/__integer"
        },
        {
          "description": "The maximum number of items to include in each page of the response. The default value is 50."
        }
      ]
    },
    "nextToken": {
      "allOf": [
        {
          "$ref": "#/components/schemas/__string"
        },
        {
          "description": "The nextToken string that specifies which page of results to return in a paginated response."
        }
      ]
    },
    "sortCriteria": {
      "allOf": [
        {
          "$ref": "#/components/schemas/BucketSortCriteria"
        },
        {
          "description": "The criteria to use to sort the query results."
        }
      ]
    }
  }
}